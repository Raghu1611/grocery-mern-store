# Grocery MERN Store

This is a MERN stack online grocery application.

## Features
- User Authentication (Login, Register, Forgot Password)
- Product Listing
- Cart System
- Order Placement
- Admin Panel

## Setup
1. Backend: `cd backend` && `npm install` && `npm start`
2. Frontend: `cd frontend` && `npm install` && `npm run dev`
